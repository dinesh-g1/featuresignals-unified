package eval

import "github.com/featuresignals/server/internal/domain"

// Ruleset is a type alias kept for backward compatibility.
// The canonical definition lives in the domain package.
type Ruleset = domain.Ruleset
