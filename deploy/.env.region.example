# Regional deployment environment variables.
# Copy to .env on each regional server and fill in the values.
#
# Primary region (IN) — serves the full stack: API, Dashboard, Website, Docs, Ops Portal.
#   REGION=in
#   DOMAIN_API=api.featuresignals.com
#   DOMAIN_APP=app.featuresignals.com
#   DOMAIN_MAIN=featuresignals.com        # primary only
#   DOMAIN_DOCS=docs.featuresignals.com   # primary only
#   DOMAIN_OPS=ops.featuresignals.com     # primary only (internal admin portal)
#
# Satellite regions — serve API + Dashboard only. Website and docs are
# served from the primary region; do NOT set DOMAIN_MAIN or DOMAIN_DOCS.
#
# US region:
#   REGION=us
#   DOMAIN_API=api.us.featuresignals.com
#   DOMAIN_APP=app.us.featuresignals.com
#
# EU region:
#   REGION=eu
#   DOMAIN_API=api.eu.featuresignals.com
#   DOMAIN_APP=app.eu.featuresignals.com
#
# Dev environment (Utho Bangalore — for development/testing):
#   REGION=dev
#   DOMAIN_API=api.dev.featuresignals.com
#   DOMAIN_APP=app.dev.featuresignals.com

# --- Region ---
REGION=in
DOMAIN_API=api.featuresignals.com
DOMAIN_APP=app.featuresignals.com
# DOMAIN_MAIN and DOMAIN_DOCS are set only on the primary (IN) region.
# When set, the deploy script enables website + docs builds (--profile full).
# Satellite regions must omit these to run in API + Dashboard only mode.
DOMAIN_MAIN=featuresignals.com
DOMAIN_DOCS=docs.featuresignals.com
DOMAIN_OPS=ops.featuresignals.com

# --- PostgreSQL ---
POSTGRES_PASSWORD=
DB_ADMIN_PASSWORD=
DB_READONLY_PASSWORD=

# --- API Server ---
# CRITICAL: All regions MUST share the same JWT_SECRET for cross-region token validity.
# Generate with: openssl rand -hex 32
JWT_SECRET=
LOG_LEVEL=info

# --- Payment Gateways ---
# US/EU regions use Stripe; IN region uses PayU
PAYMENT_GATEWAY=stripe
PAYU_MERCHANT_KEY=
PAYU_SALT=
PAYU_MODE=test
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=
STRIPE_MODE=test

# --- Email (ZeptoMail) ---
ZEPTOMAIL_TOKEN=
ZEPTOMAIL_FROM_EMAIL=noreply@featuresignals.com
ZEPTOMAIL_FROM_NAME=FeatureSignals
ZEPTOMAIL_BASE_URL=https://api.zeptomail.in

# --- OpenTelemetry / SigNoz ---
OTEL_ENABLED=true
OTEL_EXPORTER_OTLP_ENDPOINT=ingest.us2.signoz.cloud:443
OTEL_INGESTION_KEY=
OTEL_SERVICE_NAME=featuresignals-api
OTEL_TRACES_ENABLED=true
OTEL_METRICS_ENABLED=true
OTEL_LOGS_ENABLED=false
OTEL_LOG_LEVEL=warn
OTEL_TRACE_SAMPLE_RATE=0.1
